import 'package:flutter/material.dart';

class ComplaintsView extends StatelessWidget {
  const ComplaintsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Complaints Screen", style: TextStyle(fontSize: 22)),
      ),
    );
  }
}
